<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    
    <title><%= content_for?(:title) ? yield(:title) : "React Store" %></title>

    <%= stylesheet_link_tag "application" %>
    
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="wrapper">
      <%= render "common/header" %>
      <div class="content">
        <div class="row">
          <div class="small-12 column">
            <%= yield %>    
          </div>
        </div>
      </div>        
    </div>   
  </body>
  <%= javascript_include_tag "application"%>
</html>
